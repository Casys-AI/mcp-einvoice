/**
 * E-Invoice Business Entity Tools
 *
 * REMOVED: Business entity and enrollment endpoints are not part of the
 * verified Iopole Swagger spec. These tools were speculative and have been
 * removed to avoid exposing non-existent API endpoints.
 *
 * @module lib/einvoice/tools/business-entity
 */

import type { EInvoiceTool } from "./types.ts";

export const businessEntityTools: EInvoiceTool[] = [];
